const HexConfig = {
	// -------------------------
	// Game board settings
	// -------------------------
	/** Diameter for one hexagon */
	HexSize: 60,

	/** Board width (in hexagons) */
	boardWidth: 50,
	/** Board height (in hexagons) */
	boardHeight: 30,

	// -------------------------
	// Viewport settings
	// -------------------------
	clampOffset: 500,
};

export default HexConfig;
